/**
 * Shared history-scroll thresholds owned outside the controller implementation.
 * Keeping this contract in a small module prevents controller maintenance from
 * accidentally deleting the declaration together with nearby comments.
 */
export const HISTORY_AUTO_LOAD_THRESHOLD = 8;
